// Test stub for `astro:env/server`. The unit project aliases the
// virtual module here so action code can be imported under vitest.
// Default values represent a *valid* environment; tests that need
// invalid values use `vi.mock('astro:env/server', …)` to override
// per suite.
export const RESEND_API_KEY = 're_test_key';
export const SENDER_EMAIL = 'sender@example.com';
export const RECIPIENT_EMAIL = 'recipient@example.com';
