---
import { Hero, type HeroProps } from '../components/Hero/Hero';
import { type KeySkill, KeySkills } from '../components/KeySkills/KeySkills';

interface Props {
  hero: HeroProps;
  keySkills: KeySkill[];
}

const { hero, keySkills } = Astro.props;
---

<main>
  <Hero {...hero} />
  <KeySkills skills={keySkills} />
  <slot />
</main>

<style>
  main {
    max-width: var(--container-max);
    margin: 0 auto;
    padding: 0 var(--container-pad) 100px;
  }
</style>
