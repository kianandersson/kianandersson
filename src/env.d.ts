/**
 * Private contact details for the offline CV, injected at build time via Vite
 * `define` (see astro.config.mjs). A JSON string, or '' on the public build.
 */
declare const __PRINT_OPTIONS__: string;
